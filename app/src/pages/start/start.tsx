import React from "react";
import { Panel, Layout, TextField } from "../../components";

const Start = () => (
  <Layout>
    <Panel backgroundColor="bg-black-200" className="w-96">
      <TextField type="text" value="" />
    </Panel>
  </Layout>
);

export default Start;
